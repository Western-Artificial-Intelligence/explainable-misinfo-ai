Websearch shit
Google Programmable Search (Custom Search JSON API) offers 100 queries/day free.


                 ┌──────────────────────────────┐
Claim (text) --> │  0) Preprocess / Normalize   │
                 │ - clean whitespace           │
                 │ - optional language filter   │
                 └─────────────┬────────────────┘
                               │
                               ▼
                 ┌──────────────────────────────┐
                 │ 1) Verifier (RoBERTa)        │
                 │ Input: claim (+ article opt.)│
                 │ Output: label + score + conf │
                 └─────────────┬────────────────┘
                               │
                         (never skip RAG)
                               │
                               ▼
                 ┌──────────────────────────────┐
                 │ 2) RAG Evidence Gathering    │
                 │ 2.1 Query Builder            │
                 │ 2.2 Search (Google PSE API)  │
                 │ 2.3 Fetch top URLs           │
                 │ 2.4 Clean + Chunk            │
                 │ 2.5 Retrieve + Rerank chunks │
                 │ Output: top evidence snippets│
                 │         + sources/URLs       │
                 └─────────────┬────────────────┘
                               │
                               ▼
                 ┌──────────────────────────────┐
                 │ 3) Guardrail (Evidence Check)│
                 │ - enough evidence?           │
                 │ - supports vs contradicts?   │
                 │ - conflicts? -> MIXED        │
                 └───────┬───────────┬──────────┘
                         │enough     │no/weak
                         ▼           ▼
          ┌──────────────────────┐   ┌──────────────────────────┐
          │ 4) Generator LLM     │   │ Output: INSUFFICIENT_    │
          │ (one model, 2 modes) │   │ EVIDENCE + show snippets │
          | ollama/chatGPT/etc.  |   |                          |
          └──────────┬───────────┘   └──────────────────────────┘
                     │
        ┌────────────┴─────────────┐
        ▼                          ▼
┌──────────────────────┐   ┌────────────────────────┐
│ Mode A: WHY_FALSE    │   │ Mode B: WHAT_TRUE      │
│ Input: claim+evidence│   │ Input: claim+evidence  │
│ Output: explanation  │   │ Output: corrected claim│
│ + citations (E1,E2..)│   │ + support + citations  │
└──────────┬───────────┘   └───────────┬────────────┘
           │                           │
           └──────────────┬────────────┘
                          ▼
                ┌──────────────────────────────┐
                │ 5) Final Packager            │
                │ JSON: label,score,conf,      │
                │ explanation/correction,      │
                │ evidence_used:[E1..], urls   │
                └──────────────────────────────┘

