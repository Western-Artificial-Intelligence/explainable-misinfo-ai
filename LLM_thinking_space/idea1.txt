we need 2 text-generation models:
- why it's false
- what's true

just create 1 model then feed different system command

Explanation + Rag + @
after train, implement web search.

Why-false mode
Inputs: claim + (optional article) + classifier result + RAG evidence + (optional explainability highlights)
Output: a grounded explanation of why the claim is wrong/misleading.

What’s-true mode (correction)
Inputs: claim + RAG evidence
Output: corrected statement + short justification + citations.

Explainability module = gives model-driven highlights (tokens/spans) you can feed into the LLM or show separately.
RAG module = fetches evidence snippets the LLM must rely on.
@ modules (your scoring/classifier, formatting, routing logic, etc.) = decide which mode to call and how to structure output.

