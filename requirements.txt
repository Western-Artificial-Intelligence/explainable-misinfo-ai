# Core model/training libs
torch>=2.0.0
transformers>=4.30.0,<5
datasets>=2.0.0
numpy>=1.24.0,<2
scikit-learn>=1.3.0
pandas>=2.0.0
peft
pyyaml
tqdm
accelerate
evaluate
kagglehub

# API/runtime (required for local FastAPI + frontend integration)
fastapi
uvicorn[standard]
python-dotenv
pydantic
httpx
python-multipart>=0.0.6
aiofiles>=23.2.0

# UI
streamlit>=1.28.0

# Optional heavy dependencies (audio/image/explainability) are in:
# requirements-optional.txt
